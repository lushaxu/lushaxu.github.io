# Source material for the GradQuant page — contains student names, emails and IDs.
# Never publish: only aggregate counts from these files appear on the site.
temp/

# macOS
.DS_Store

# Local Claude Code dev config (preview server, etc.)
.claude/
